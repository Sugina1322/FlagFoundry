{
  "name": "flagfoundry-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "prisma:generate": "prisma generate --schema db/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema db/schema.prisma",
    "seed": "node db/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
